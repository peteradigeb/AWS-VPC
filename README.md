# AWS-VPC
Creating a VPC and adding its components like IGW, SUBNETS opening(denying and allowing) ports on the subnets(securing groups) and NACL level attached and EC2 instances deployed a python server on it allowing all-traffic and on port 8000 as seen in the images
